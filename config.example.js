module.exports = {
  // Summary server host
  summaryHost: "http://localhost:5050",
  // Google Cloud Project ID & API key (where speech-to-text API is enabled)
  projectId: "your-own-project-id",
  keyFilename: "../your-own-key-filename.json",
}

